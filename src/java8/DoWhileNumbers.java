package java8;

public class DoWhileNumbers {
    public static void main(String[] args) {
        /*double number = 100;
        do {
            if (number%1==0) {
                System.out.println(number);
                number = number + (-0.5);  //or number-=0.5;
            }
            else
                number = new int[number];
                System.out.println(int number);
                number = number + (-0.5);
        }
        while (number>=0);

         */
    }
}
